<script>
    // See Index.js to see how to show this messages.

    var appResources = appResources || {};

    // Messages (!!IMPORTANT When this get big, consider to move the messages to specific views using the scripts section )
    appResources.message1 = '@GlobalResources["error1"]';
    appResources.message2 = '@GlobalResources["error2"]';

    // Url
    appResources.url1 = '@Url.Action("Index", "Home")';
    appResources.url2 = '@Url.Action("Index", "Home", new { id = 1 })';

</script>
